name: absensi
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Paket yang benar untuk lokalisasi
  flutter_localizations:
    sdk: flutter

  # Paket-paket lain Anda
  http: ^1.2.1
  image_picker: ^1.1.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  camera: ^0.11.0+1
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  google_maps_flutter: ^2.6.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/